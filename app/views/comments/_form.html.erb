<div class= "text-center">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
      <%= simple_form_for [@post, @post.comments.build] do |f| %>
        <%= f.input :content %>
        <%= f.submit "Comment", class: "btn btn-bro" %>
      <% end %>
      </div>
    </div>
  </div>
</div>
